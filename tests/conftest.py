"""
Shared pytest configuration for FreelancerToolkit tests.

Placing this file here makes the `tests/` directory a proper pytest root so
all test_*.py files are automatically discovered, regardless of the working
directory from which pytest is invoked.
"""
